git init // for initializing repository für den Start 
git add  <file>
git commit -m "your commit message -m for massage"
 
 " git add ." => Betrachtet den gesamten Ordner und addet sie.
 
 git reset 

 git reset --soft HEAD~1 => setzt auf Saged Changes zurück

 git reset --mixed HEAD~1

 git reset --hard HEAD~1

